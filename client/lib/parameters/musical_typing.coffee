#global key bindings
class Binding

    # Mousetrap.bind "shift+down", =>
    #   if @octave > -2
    #     @octave -= 1

    # Mousetrap.bind "shift+up", =>
    #   if @octave < 2
    #     @octave += 1

    # Mousetrap.bind "?", =>
    #     alert 'keyboard shortcuts'
